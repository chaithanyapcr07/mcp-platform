# Postgres

Docker Compose starts PostgreSQL 16 for local runtime state. Git-backed registry definitions live in `registry/`; live audit logs, access approvals, task executions, rate limit counters, and secret references live in Postgres.
