# Seed

Runtime seed data is implemented in `apps/api/prisma/seed.ts` so it can populate roles, permissions, users, projects, connector access, skill access, task access, registry rows, policy rows, and secret references through Prisma.
