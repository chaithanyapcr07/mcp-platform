# Custom Team-Owned Connector Template

Use this template when a team owns the runtime and needs platform registration, policy, and audit by default.

```bash
npm run create:connector -- --name my-team-connector --template custom-team-owned
```
