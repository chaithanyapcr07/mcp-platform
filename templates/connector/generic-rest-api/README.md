# Generic REST API Connector Template

Use this template when a team wants to expose a governed REST API as MCP tools through the gateway.

```bash
npm run create:connector -- --name my-rest-connector --template generic-rest-api
```
